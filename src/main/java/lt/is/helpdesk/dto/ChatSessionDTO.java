package lt.is.helpdesk.dto;

public class ChatSessionDTO {
    private Long id;

    public ChatSessionDTO(Long id) {
        this.id = id;
    }

    public Long getId() {
        return id;
    }
}
